// import { DefinitionProvider, TextDocument, Position, CancellationToken, Location, Uri } from 'vscode';

// export class ElementDefinitionProvider implements DefinitionProvider {
//     private _position!: Position;
//     private _document!: TextDocument;
//     private _token!: CancellationToken;

//     provideDefinition(document: TextDocument, position: Position, token: CancellationToken): Thenable<Location>  {
//         return new Location(Uri.file('/'), new Position(0, 0));
//     }

// }
